version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: nip-transfer-db
    environment:
      POSTGRES_DB: nip_transfer_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: nip-transfer-service
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DB_URL: jdbc:postgresql://postgres:5432/nip_transfer_db
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
      API_KEY: dev-api-key-change-in-production
    ports:
      - "8080:8080"
    restart: unless-stopped

volumes:
  postgres_data:
